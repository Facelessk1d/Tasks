// second task

const currentYear = 2026
const birthYear = 2010

const beyondBirthday = (date1, date2) => {
    const res = date2 - date1;
    return  console.log(res);
}

beyondBirthday(birthYear, currentYear)