// Проверка четности: Запроси у пользователя число. Определи и выведи, 
//является ли число четным (используй оператор % — остаток от деления).

const isEvenFunc = () => {
    const num = prompt("введите число")
    const funj = num % 2
    
    if (funj == 0) {    
        alert("число четное");   
    }

    else {
        alert("outworld destroyer from dota 2 ")
    }
    
}
isEvenFunc()